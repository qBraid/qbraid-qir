name: Formatting check

on:
  pull_request:
      branches: ['main']
  workflow_dispatch:

jobs:
  main:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install -e '.[lint]'
          python3 -m pip install --upgrade black
      - name: Check isort, black, headers
        run: |
          black qbraid_qir tests tools examples --verbose --check
          isort --check-only qbraid_qir tests tools
          qbraid admin headers qbraid_qir tests